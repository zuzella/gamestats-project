# AI Documentation

## Case 1: Wrong package installation
AI initially suggested writing `pip install pandas` inside the Python file.
This caused a syntax error.
We fixed it by running the installation command in the terminal instead.

## Case 2: File path issue
The dataset was not loading because the folder name/path was incorrect.
We debugged the directory structure and corrected the dataset location.

## Case 3: CSS styling issue
After modifying the Streamlit design, the dropdown text became unreadable.
We fixed the CSS so the selected team names display in black and the background styling remained visible.
